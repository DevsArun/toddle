<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Deliberately no permissions.
      No internet, no storage, no camera, no microphone, no location.
      A kids app with zero permissions passes review far more easily.

      The Amazon in-app purchase receiver is NOT declared here. It is inserted
      by tools/enable_amazon_iap.sh together with the Amazon SDK jar. Declaring
      a receiver whose class does not exist can block installation on ordinary
      Android phones, which would make free testing impossible.
    -->

    <application
        android:label="Baby Coloring"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:allowBackup="true">

        <activity
            android:name="com.itschool.toddler_coloring.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:screenOrientation="fullSensor"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
